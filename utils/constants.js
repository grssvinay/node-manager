const HTTP_STATUS_CODES = {
  OK: 200
}

const HTTP_STATUS_MESSAGES = {
  OK: "OK"
}

const CATCHUP_ENV = {
  DEFAULT: "mainnet",
  MAINNET: "mainnet",
  BETANET: "betanet",
  TESTNET: "testnet",
}

const DEFAULT_PORT = 3000

module.exports = {
  HTTP_STATUS_CODES,
  HTTP_STATUS_MESSAGES,
  DEFAULT_PORT,
  CATCHUP_ENV
}